/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  none,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  option,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  PluginAuthorityPair,
  PluginAuthorityPairArgs,
  getPluginAuthorityPairSerializer,
} from '../types';

// Accounts.
export type CreateCollectionV1InstructionAccounts = {
  /** The address of the new asset */
  collection: Signer;
  /** The authority of the new asset */
  updateAuthority?: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateCollectionV1InstructionData = {
  discriminator: number;
  name: string;
  uri: string;
  plugins: Option<Array<PluginAuthorityPair>>;
};

export type CreateCollectionV1InstructionDataArgs = {
  name: string;
  uri: string;
  plugins?: OptionOrNullable<Array<PluginAuthorityPairArgs>>;
};

export function getCreateCollectionV1InstructionDataSerializer(): Serializer<
  CreateCollectionV1InstructionDataArgs,
  CreateCollectionV1InstructionData
> {
  return mapSerializer<
    CreateCollectionV1InstructionDataArgs,
    any,
    CreateCollectionV1InstructionData
  >(
    struct<CreateCollectionV1InstructionData>(
      [
        ['discriminator', u8()],
        ['name', string()],
        ['uri', string()],
        ['plugins', option(array(getPluginAuthorityPairSerializer()))],
      ],
      { description: 'CreateCollectionV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: 1,
      plugins: value.plugins ?? none(),
    })
  ) as Serializer<
    CreateCollectionV1InstructionDataArgs,
    CreateCollectionV1InstructionData
  >;
}

// Args.
export type CreateCollectionV1InstructionArgs =
  CreateCollectionV1InstructionDataArgs;

// Instruction.
export function createCollectionV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: CreateCollectionV1InstructionAccounts &
    CreateCollectionV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    collection: {
      index: 0,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    updateAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.updateAuthority ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateCollectionV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateCollectionV1InstructionDataSerializer().serialize(
    resolvedArgs as CreateCollectionV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
