/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  BasePluginAdapterKey,
  BasePluginAdapterKeyArgs,
  getBasePluginAdapterKeySerializer,
} from '../types';

// Accounts.
export type RemoveCollectionPluginAdapterV1InstructionAccounts = {
  /** The address of the asset */
  collection: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The owner or delegate of the asset */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
  /** The SPL Noop Program */
  logWrapper?: PublicKey | Pda;
};

// Data.
export type RemoveCollectionPluginAdapterV1InstructionData = {
  discriminator: number;
  key: BasePluginAdapterKey;
};

export type RemoveCollectionPluginAdapterV1InstructionDataArgs = {
  key: BasePluginAdapterKeyArgs;
};

export function getRemoveCollectionPluginAdapterV1InstructionDataSerializer(): Serializer<
  RemoveCollectionPluginAdapterV1InstructionDataArgs,
  RemoveCollectionPluginAdapterV1InstructionData
> {
  return mapSerializer<
    RemoveCollectionPluginAdapterV1InstructionDataArgs,
    any,
    RemoveCollectionPluginAdapterV1InstructionData
  >(
    struct<RemoveCollectionPluginAdapterV1InstructionData>(
      [
        ['discriminator', u8()],
        ['key', getBasePluginAdapterKeySerializer()],
      ],
      { description: 'RemoveCollectionPluginAdapterV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 25 })
  ) as Serializer<
    RemoveCollectionPluginAdapterV1InstructionDataArgs,
    RemoveCollectionPluginAdapterV1InstructionData
  >;
}

// Args.
export type RemoveCollectionPluginAdapterV1InstructionArgs =
  RemoveCollectionPluginAdapterV1InstructionDataArgs;

// Instruction.
export function removeCollectionPluginAdapterV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: RemoveCollectionPluginAdapterV1InstructionAccounts &
    RemoveCollectionPluginAdapterV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    collection: {
      index: 0,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    logWrapper: {
      index: 4,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveCollectionPluginAdapterV1InstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRemoveCollectionPluginAdapterV1InstructionDataSerializer().serialize(
      resolvedArgs as RemoveCollectionPluginAdapterV1InstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
