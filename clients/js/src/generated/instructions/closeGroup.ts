/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseGroupInstructionAccounts = {
  /** The update authority receiving lamports */
  updateAuthority: Signer;
  /** The group account to close */
  group: PublicKey | Pda;
};

// Data.
export type CloseGroupInstructionData = { discriminator: number };

export type CloseGroupInstructionDataArgs = {};

export function getCloseGroupInstructionDataSerializer(): Serializer<
  CloseGroupInstructionDataArgs,
  CloseGroupInstructionData
> {
  return mapSerializer<
    CloseGroupInstructionDataArgs,
    any,
    CloseGroupInstructionData
  >(
    struct<CloseGroupInstructionData>([['discriminator', u8()]], {
      description: 'CloseGroupInstructionData',
    }),
    (value) => ({ ...value, discriminator: 33 })
  ) as Serializer<CloseGroupInstructionDataArgs, CloseGroupInstructionData>;
}

// Instruction.
export function closeGroup(
  context: Pick<Context, 'programs'>,
  input: CloseGroupInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    updateAuthority: {
      index: 0,
      isWritable: true as boolean,
      value: input.updateAuthority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseGroupInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
