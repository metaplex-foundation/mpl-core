/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  string,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  PluginAuthorityType,
  PluginAuthorityTypeArgs,
  getPluginAuthorityTypeSerializer,
} from '../types';

// Accounts.
export type AddGroupPluginInstructionAccounts = {
  /** The group update authority or delegated authority */
  authority?: Signer;
  /** The group account */
  group: PublicKey | Pda;
  /** The plugin account to create */
  plugin: PublicKey | Pda;
  /** The plugin program id */
  pluginProgram: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type AddGroupPluginInstructionData = {
  discriminator: number;
  pluginType: string;
  authorityType: PluginAuthorityType;
  pluginArgs: Uint8Array;
};

export type AddGroupPluginInstructionDataArgs = {
  pluginType: string;
  authorityType: PluginAuthorityTypeArgs;
  pluginArgs: Uint8Array;
};

export function getAddGroupPluginInstructionDataSerializer(): Serializer<
  AddGroupPluginInstructionDataArgs,
  AddGroupPluginInstructionData
> {
  return mapSerializer<
    AddGroupPluginInstructionDataArgs,
    any,
    AddGroupPluginInstructionData
  >(
    struct<AddGroupPluginInstructionData>(
      [
        ['discriminator', u8()],
        ['pluginType', string()],
        ['authorityType', getPluginAuthorityTypeSerializer()],
        ['pluginArgs', bytes({ size: u32() })],
      ],
      { description: 'AddGroupPluginInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 42 })
  ) as Serializer<
    AddGroupPluginInstructionDataArgs,
    AddGroupPluginInstructionData
  >;
}

// Args.
export type AddGroupPluginInstructionArgs = AddGroupPluginInstructionDataArgs;

// Instruction.
export function addGroupPlugin(
  context: Pick<Context, 'identity' | 'programs'>,
  input: AddGroupPluginInstructionAccounts & AddGroupPluginInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    plugin: {
      index: 2,
      isWritable: true as boolean,
      value: input.plugin ?? null,
    },
    pluginProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.pluginProgram ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddGroupPluginInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddGroupPluginInstructionDataSerializer().serialize(
    resolvedArgs as AddGroupPluginInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
