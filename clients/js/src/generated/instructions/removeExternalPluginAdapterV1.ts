/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  BaseExternalPluginAdapterKey,
  BaseExternalPluginAdapterKeyArgs,
  getBaseExternalPluginAdapterKeySerializer,
} from '../types';

// Accounts.
export type RemoveExternalPluginAdapterV1InstructionAccounts = {
  /** The address of the asset */
  asset: PublicKey | Pda;
  /** The collection to which the asset belongs */
  collection?: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The owner or delegate of the asset */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
  /** The SPL Noop Program */
  logWrapper?: PublicKey | Pda;
};

// Data.
export type RemoveExternalPluginAdapterV1InstructionData = {
  discriminator: number;
  key: BaseExternalPluginAdapterKey;
};

export type RemoveExternalPluginAdapterV1InstructionDataArgs = {
  key: BaseExternalPluginAdapterKeyArgs;
};

export function getRemoveExternalPluginAdapterV1InstructionDataSerializer(): Serializer<
  RemoveExternalPluginAdapterV1InstructionDataArgs,
  RemoveExternalPluginAdapterV1InstructionData
> {
  return mapSerializer<
    RemoveExternalPluginAdapterV1InstructionDataArgs,
    any,
    RemoveExternalPluginAdapterV1InstructionData
  >(
    struct<RemoveExternalPluginAdapterV1InstructionData>(
      [
        ['discriminator', u8()],
        ['key', getBaseExternalPluginAdapterKeySerializer()],
      ],
      { description: 'RemoveExternalPluginAdapterV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 24 })
  ) as Serializer<
    RemoveExternalPluginAdapterV1InstructionDataArgs,
    RemoveExternalPluginAdapterV1InstructionData
  >;
}

// Args.
export type RemoveExternalPluginAdapterV1InstructionArgs =
  RemoveExternalPluginAdapterV1InstructionDataArgs;

// Instruction.
export function removeExternalPluginAdapterV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: RemoveExternalPluginAdapterV1InstructionAccounts &
    RemoveExternalPluginAdapterV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    asset: {
      index: 0,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    collection: {
      index: 1,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    logWrapper: {
      index: 5,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveExternalPluginAdapterV1InstructionArgs = {
    ...input,
  };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRemoveExternalPluginAdapterV1InstructionDataSerializer().serialize(
      resolvedArgs as RemoveExternalPluginAdapterV1InstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
