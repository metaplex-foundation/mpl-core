/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RemoveCollectionsFromGroupInstructionAccounts = {
  /** The group update authority or delegated authority */
  authority?: Signer;
  /** The group account */
  group: PublicKey | Pda;
};

// Data.
export type RemoveCollectionsFromGroupInstructionData = {
  discriminator: number;
  collectionIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export type RemoveCollectionsFromGroupInstructionDataArgs = {
  collectionIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export function getRemoveCollectionsFromGroupInstructionDataSerializer(): Serializer<
  RemoveCollectionsFromGroupInstructionDataArgs,
  RemoveCollectionsFromGroupInstructionData
> {
  return mapSerializer<
    RemoveCollectionsFromGroupInstructionDataArgs,
    any,
    RemoveCollectionsFromGroupInstructionData
  >(
    struct<RemoveCollectionsFromGroupInstructionData>(
      [
        ['discriminator', u8()],
        ['collectionIndices', bytes({ size: u32() })],
        ['authorityIndices', bytes({ size: u32() })],
      ],
      { description: 'RemoveCollectionsFromGroupInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 36 })
  ) as Serializer<
    RemoveCollectionsFromGroupInstructionDataArgs,
    RemoveCollectionsFromGroupInstructionData
  >;
}

// Args.
export type RemoveCollectionsFromGroupInstructionArgs =
  RemoveCollectionsFromGroupInstructionDataArgs;

// Instruction.
export function removeCollectionsFromGroup(
  context: Pick<Context, 'identity' | 'programs'>,
  input: RemoveCollectionsFromGroupInstructionAccounts &
    RemoveCollectionsFromGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveCollectionsFromGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getRemoveCollectionsFromGroupInstructionDataSerializer().serialize(
      resolvedArgs as RemoveCollectionsFromGroupInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
