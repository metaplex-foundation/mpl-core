/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RemoveGroupPluginInstructionAccounts = {
  /** The group update authority or delegated authority */
  authority?: Signer;
  /** The group account */
  group: PublicKey | Pda;
  /** The plugin account to remove */
  plugin: PublicKey | Pda;
};

// Data.
export type RemoveGroupPluginInstructionData = { discriminator: number };

export type RemoveGroupPluginInstructionDataArgs = {};

export function getRemoveGroupPluginInstructionDataSerializer(): Serializer<
  RemoveGroupPluginInstructionDataArgs,
  RemoveGroupPluginInstructionData
> {
  return mapSerializer<
    RemoveGroupPluginInstructionDataArgs,
    any,
    RemoveGroupPluginInstructionData
  >(
    struct<RemoveGroupPluginInstructionData>([['discriminator', u8()]], {
      description: 'RemoveGroupPluginInstructionData',
    }),
    (value) => ({ ...value, discriminator: 42 })
  ) as Serializer<
    RemoveGroupPluginInstructionDataArgs,
    RemoveGroupPluginInstructionData
  >;
}

// Instruction.
export function removeGroupPlugin(
  context: Pick<Context, 'identity' | 'programs'>,
  input: RemoveGroupPluginInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    plugin: {
      index: 2,
      isWritable: true as boolean,
      value: input.plugin ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveGroupPluginInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
