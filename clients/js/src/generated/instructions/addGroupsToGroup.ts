/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddGroupsToGroupInstructionAccounts = {
  /** The parent group update authority or delegated authority */
  authority?: Signer;
  /** The parent group account */
  parentGroup: PublicKey | Pda;
};

// Data.
export type AddGroupsToGroupInstructionData = {
  discriminator: number;
  groupIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export type AddGroupsToGroupInstructionDataArgs = {
  groupIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export function getAddGroupsToGroupInstructionDataSerializer(): Serializer<
  AddGroupsToGroupInstructionDataArgs,
  AddGroupsToGroupInstructionData
> {
  return mapSerializer<
    AddGroupsToGroupInstructionDataArgs,
    any,
    AddGroupsToGroupInstructionData
  >(
    struct<AddGroupsToGroupInstructionData>(
      [
        ['discriminator', u8()],
        ['groupIndices', bytes({ size: u32() })],
        ['authorityIndices', bytes({ size: u32() })],
      ],
      { description: 'AddGroupsToGroupInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 40 })
  ) as Serializer<
    AddGroupsToGroupInstructionDataArgs,
    AddGroupsToGroupInstructionData
  >;
}

// Args.
export type AddGroupsToGroupInstructionArgs =
  AddGroupsToGroupInstructionDataArgs;

// Instruction.
export function addGroupsToGroup(
  context: Pick<Context, 'identity' | 'programs'>,
  input: AddGroupsToGroupInstructionAccounts & AddGroupsToGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    parentGroup: {
      index: 1,
      isWritable: true as boolean,
      value: input.parentGroup ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddGroupsToGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddGroupsToGroupInstructionDataSerializer().serialize(
    resolvedArgs as AddGroupsToGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
