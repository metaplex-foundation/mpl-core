/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateGroupMetadataInstructionAccounts = {
  /** The current update authority or delegated authority */
  authority?: Signer;
  /** The group account to update */
  group: PublicKey | Pda;
};

// Data.
export type UpdateGroupMetadataInstructionData = {
  discriminator: number;
  name: Option<string>;
  uri: Option<string>;
  newUpdateAuthority: Option<PublicKey>;
};

export type UpdateGroupMetadataInstructionDataArgs = {
  name: OptionOrNullable<string>;
  uri: OptionOrNullable<string>;
  newUpdateAuthority: OptionOrNullable<PublicKey>;
};

export function getUpdateGroupMetadataInstructionDataSerializer(): Serializer<
  UpdateGroupMetadataInstructionDataArgs,
  UpdateGroupMetadataInstructionData
> {
  return mapSerializer<
    UpdateGroupMetadataInstructionDataArgs,
    any,
    UpdateGroupMetadataInstructionData
  >(
    struct<UpdateGroupMetadataInstructionData>(
      [
        ['discriminator', u8()],
        ['name', option(string())],
        ['uri', option(string())],
        ['newUpdateAuthority', option(publicKeySerializer())],
      ],
      { description: 'UpdateGroupMetadataInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 34 })
  ) as Serializer<
    UpdateGroupMetadataInstructionDataArgs,
    UpdateGroupMetadataInstructionData
  >;
}

// Args.
export type UpdateGroupMetadataInstructionArgs =
  UpdateGroupMetadataInstructionDataArgs;

// Instruction.
export function updateGroupMetadata(
  context: Pick<Context, 'identity' | 'programs'>,
  input: UpdateGroupMetadataInstructionAccounts &
    UpdateGroupMetadataInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateGroupMetadataInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateGroupMetadataInstructionDataSerializer().serialize(
    resolvedArgs as UpdateGroupMetadataInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
