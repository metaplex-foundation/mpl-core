/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddGroupsToGroupV1InstructionAccounts = {
  /** The address of the parent group to modify */
  parentGroup: PublicKey | Pda;
  /** The account paying for storage fees */
  payer?: Signer;
  /** The update authority or delegate of the groups */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type AddGroupsToGroupV1InstructionData = {
  discriminator: number;
  groups: Array<PublicKey>;
};

export type AddGroupsToGroupV1InstructionDataArgs = {
  groups: Array<PublicKey>;
};

export function getAddGroupsToGroupV1InstructionDataSerializer(): Serializer<
  AddGroupsToGroupV1InstructionDataArgs,
  AddGroupsToGroupV1InstructionData
> {
  return mapSerializer<
    AddGroupsToGroupV1InstructionDataArgs,
    any,
    AddGroupsToGroupV1InstructionData
  >(
    struct<AddGroupsToGroupV1InstructionData>(
      [
        ['discriminator', u8()],
        ['groups', array(publicKeySerializer())],
      ],
      { description: 'AddGroupsToGroupV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 37 })
  ) as Serializer<
    AddGroupsToGroupV1InstructionDataArgs,
    AddGroupsToGroupV1InstructionData
  >;
}

// Args.
export type AddGroupsToGroupV1InstructionArgs =
  AddGroupsToGroupV1InstructionDataArgs;

// Instruction.
export function addGroupsToGroupV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: AddGroupsToGroupV1InstructionAccounts &
    AddGroupsToGroupV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    parentGroup: {
      index: 0,
      isWritable: true as boolean,
      value: input.parentGroup ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddGroupsToGroupV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddGroupsToGroupV1InstructionDataSerializer().serialize(
    resolvedArgs as AddGroupsToGroupV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
