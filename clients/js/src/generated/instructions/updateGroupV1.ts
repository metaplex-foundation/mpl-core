/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  option,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdateGroupV1InstructionAccounts = {
  /** The address of the group to update */
  group: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The update authority or update delegate of the group */
  authority?: Signer;
  /** The new update authority of the group */
  newUpdateAuthority?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type UpdateGroupV1InstructionData = {
  discriminator: number;
  newName: Option<string>;
  newUri: Option<string>;
};

export type UpdateGroupV1InstructionDataArgs = {
  newName: OptionOrNullable<string>;
  newUri: OptionOrNullable<string>;
};

export function getUpdateGroupV1InstructionDataSerializer(): Serializer<
  UpdateGroupV1InstructionDataArgs,
  UpdateGroupV1InstructionData
> {
  return mapSerializer<
    UpdateGroupV1InstructionDataArgs,
    any,
    UpdateGroupV1InstructionData
  >(
    struct<UpdateGroupV1InstructionData>(
      [
        ['discriminator', u8()],
        ['newName', option(string())],
        ['newUri', option(string())],
      ],
      { description: 'UpdateGroupV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 49 })
  ) as Serializer<
    UpdateGroupV1InstructionDataArgs,
    UpdateGroupV1InstructionData
  >;
}

// Args.
export type UpdateGroupV1InstructionArgs = UpdateGroupV1InstructionDataArgs;

// Instruction.
export function updateGroupV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: UpdateGroupV1InstructionAccounts & UpdateGroupV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    newUpdateAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.newUpdateAuthority ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateGroupV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateGroupV1InstructionDataSerializer().serialize(
    resolvedArgs as UpdateGroupV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
