/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddCollectionsToGroupInstructionAccounts = {
  /** The group update authority or delegated authority */
  authority?: Signer;
  /** The group account */
  group: PublicKey | Pda;
};

// Data.
export type AddCollectionsToGroupInstructionData = {
  discriminator: number;
  collectionIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export type AddCollectionsToGroupInstructionDataArgs = {
  collectionIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export function getAddCollectionsToGroupInstructionDataSerializer(): Serializer<
  AddCollectionsToGroupInstructionDataArgs,
  AddCollectionsToGroupInstructionData
> {
  return mapSerializer<
    AddCollectionsToGroupInstructionDataArgs,
    any,
    AddCollectionsToGroupInstructionData
  >(
    struct<AddCollectionsToGroupInstructionData>(
      [
        ['discriminator', u8()],
        ['collectionIndices', bytes({ size: u32() })],
        ['authorityIndices', bytes({ size: u32() })],
      ],
      { description: 'AddCollectionsToGroupInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 36 })
  ) as Serializer<
    AddCollectionsToGroupInstructionDataArgs,
    AddCollectionsToGroupInstructionData
  >;
}

// Args.
export type AddCollectionsToGroupInstructionArgs =
  AddCollectionsToGroupInstructionDataArgs;

// Instruction.
export function addCollectionsToGroup(
  context: Pick<Context, 'identity' | 'programs'>,
  input: AddCollectionsToGroupInstructionAccounts &
    AddCollectionsToGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddCollectionsToGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddCollectionsToGroupInstructionDataSerializer().serialize(
    resolvedArgs as AddCollectionsToGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
