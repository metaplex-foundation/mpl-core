/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddAssetsToGroupInstructionAccounts = {
  /** The group update authority or delegated authority */
  authority?: Signer;
  /** The group account */
  group: PublicKey | Pda;
};

// Data.
export type AddAssetsToGroupInstructionData = {
  discriminator: number;
  assetIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export type AddAssetsToGroupInstructionDataArgs = {
  assetIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export function getAddAssetsToGroupInstructionDataSerializer(): Serializer<
  AddAssetsToGroupInstructionDataArgs,
  AddAssetsToGroupInstructionData
> {
  return mapSerializer<
    AddAssetsToGroupInstructionDataArgs,
    any,
    AddAssetsToGroupInstructionData
  >(
    struct<AddAssetsToGroupInstructionData>(
      [
        ['discriminator', u8()],
        ['assetIndices', bytes({ size: u32() })],
        ['authorityIndices', bytes({ size: u32() })],
      ],
      { description: 'AddAssetsToGroupInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 37 })
  ) as Serializer<
    AddAssetsToGroupInstructionDataArgs,
    AddAssetsToGroupInstructionData
  >;
}

// Args.
export type AddAssetsToGroupInstructionArgs =
  AddAssetsToGroupInstructionDataArgs;

// Instruction.
export function addAssetsToGroup(
  context: Pick<Context, 'identity' | 'programs'>,
  input: AddAssetsToGroupInstructionAccounts & AddAssetsToGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddAssetsToGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddAssetsToGroupInstructionDataSerializer().serialize(
    resolvedArgs as AddAssetsToGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
