/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseGroupV1InstructionAccounts = {
  /** The address of the group to close */
  group: PublicKey | Pda;
  /** The account receiving reclaimed lamports */
  payer?: Signer;
  /** The update authority or update delegate of the group */
  authority?: Signer;
};

// Data.
export type CloseGroupV1InstructionData = { discriminator: number };

export type CloseGroupV1InstructionDataArgs = {};

export function getCloseGroupV1InstructionDataSerializer(): Serializer<
  CloseGroupV1InstructionDataArgs,
  CloseGroupV1InstructionData
> {
  return mapSerializer<
    CloseGroupV1InstructionDataArgs,
    any,
    CloseGroupV1InstructionData
  >(
    struct<CloseGroupV1InstructionData>([['discriminator', u8()]], {
      description: 'CloseGroupV1InstructionData',
    }),
    (value) => ({ ...value, discriminator: 48 })
  ) as Serializer<CloseGroupV1InstructionDataArgs, CloseGroupV1InstructionData>;
}

// Instruction.
export function closeGroupV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: CloseGroupV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseGroupV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
