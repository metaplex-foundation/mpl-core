/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RemoveAssetsFromGroupInstructionAccounts = {
  /** The group update authority or delegated authority */
  authority?: Signer;
  /** The group account */
  group: PublicKey | Pda;
};

// Data.
export type RemoveAssetsFromGroupInstructionData = {
  discriminator: number;
  assetIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export type RemoveAssetsFromGroupInstructionDataArgs = {
  assetIndices: Uint8Array;
  authorityIndices: Uint8Array;
};

export function getRemoveAssetsFromGroupInstructionDataSerializer(): Serializer<
  RemoveAssetsFromGroupInstructionDataArgs,
  RemoveAssetsFromGroupInstructionData
> {
  return mapSerializer<
    RemoveAssetsFromGroupInstructionDataArgs,
    any,
    RemoveAssetsFromGroupInstructionData
  >(
    struct<RemoveAssetsFromGroupInstructionData>(
      [
        ['discriminator', u8()],
        ['assetIndices', bytes({ size: u32() })],
        ['authorityIndices', bytes({ size: u32() })],
      ],
      { description: 'RemoveAssetsFromGroupInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 38 })
  ) as Serializer<
    RemoveAssetsFromGroupInstructionDataArgs,
    RemoveAssetsFromGroupInstructionData
  >;
}

// Args.
export type RemoveAssetsFromGroupInstructionArgs =
  RemoveAssetsFromGroupInstructionDataArgs;

// Instruction.
export function removeAssetsFromGroup(
  context: Pick<Context, 'identity' | 'programs'>,
  input: RemoveAssetsFromGroupInstructionAccounts &
    RemoveAssetsFromGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveAssetsFromGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveAssetsFromGroupInstructionDataSerializer().serialize(
    resolvedArgs as RemoveAssetsFromGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
