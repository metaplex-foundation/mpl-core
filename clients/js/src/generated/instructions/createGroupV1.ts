/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  RelationshipEntry,
  RelationshipEntryArgs,
  getRelationshipEntrySerializer,
} from '../types';

// Accounts.
export type CreateGroupV1InstructionAccounts = {
  /** The address of the new group */
  group: Signer;
  /** The authority of the new group */
  updateAuthority?: Signer;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateGroupV1InstructionData = {
  discriminator: number;
  name: string;
  uri: string;
  relationships: Array<RelationshipEntry>;
};

export type CreateGroupV1InstructionDataArgs = {
  name: string;
  uri: string;
  relationships: Array<RelationshipEntryArgs>;
};

export function getCreateGroupV1InstructionDataSerializer(): Serializer<
  CreateGroupV1InstructionDataArgs,
  CreateGroupV1InstructionData
> {
  return mapSerializer<
    CreateGroupV1InstructionDataArgs,
    any,
    CreateGroupV1InstructionData
  >(
    struct<CreateGroupV1InstructionData>(
      [
        ['discriminator', u8()],
        ['name', string()],
        ['uri', string()],
        ['relationships', array(getRelationshipEntrySerializer())],
      ],
      { description: 'CreateGroupV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 47 })
  ) as Serializer<
    CreateGroupV1InstructionDataArgs,
    CreateGroupV1InstructionData
  >;
}

// Args.
export type CreateGroupV1InstructionArgs = CreateGroupV1InstructionDataArgs;

// Instruction.
export function createGroupV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: CreateGroupV1InstructionAccounts & CreateGroupV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplCore',
    'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    updateAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.updateAuthority ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateGroupV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateGroupV1InstructionDataSerializer().serialize(
    resolvedArgs as CreateGroupV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
