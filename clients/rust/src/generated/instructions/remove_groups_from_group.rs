//! This code was AUTOGENERATED using the kinobi library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun kinobi to update it.
//!
//! [https://github.com/metaplex-foundation/kinobi]
//!

#[cfg(feature = "anchor")]
use anchor_lang::prelude::{AnchorDeserialize, AnchorSerialize};
#[cfg(not(feature = "anchor"))]
use borsh::{BorshDeserialize, BorshSerialize};

/// Accounts.
pub struct RemoveGroupsFromGroup {
    /// The parent group update authority or delegated authority
    pub authority: solana_program::pubkey::Pubkey,
    /// The parent group account
    pub parent_group: solana_program::pubkey::Pubkey,
}

impl RemoveGroupsFromGroup {
    pub fn instruction(
        &self,
        args: RemoveGroupsFromGroupInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: RemoveGroupsFromGroupInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.authority,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.parent_group,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = RemoveGroupsFromGroupInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::MPL_CORE_ID,
            accounts,
            data,
        }
    }
}

#[cfg_attr(not(feature = "anchor"), derive(BorshSerialize, BorshDeserialize))]
#[cfg_attr(feature = "anchor", derive(AnchorSerialize, AnchorDeserialize))]
pub struct RemoveGroupsFromGroupInstructionData {
    discriminator: u8,
}

impl RemoveGroupsFromGroupInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 41 }
    }
}

#[cfg_attr(not(feature = "anchor"), derive(BorshSerialize, BorshDeserialize))]
#[cfg_attr(feature = "anchor", derive(AnchorSerialize, AnchorDeserialize))]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct RemoveGroupsFromGroupInstructionArgs {
    pub group_indices: Vec<u8>,
    pub authority_indices: Vec<u8>,
}

/// Instruction builder for `RemoveGroupsFromGroup`.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[writable]` parent_group
#[derive(Default)]
pub struct RemoveGroupsFromGroupBuilder {
    authority: Option<solana_program::pubkey::Pubkey>,
    parent_group: Option<solana_program::pubkey::Pubkey>,
    group_indices: Option<Vec<u8>>,
    authority_indices: Option<Vec<u8>>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl RemoveGroupsFromGroupBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The parent group update authority or delegated authority
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_program::pubkey::Pubkey) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    /// The parent group account
    #[inline(always)]
    pub fn parent_group(&mut self, parent_group: solana_program::pubkey::Pubkey) -> &mut Self {
        self.parent_group = Some(parent_group);
        self
    }
    #[inline(always)]
    pub fn group_indices(&mut self, group_indices: Vec<u8>) -> &mut Self {
        self.group_indices = Some(group_indices);
        self
    }
    #[inline(always)]
    pub fn authority_indices(&mut self, authority_indices: Vec<u8>) -> &mut Self {
        self.authority_indices = Some(authority_indices);
        self
    }
    /// Add an aditional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = RemoveGroupsFromGroup {
            authority: self.authority.expect("authority is not set"),
            parent_group: self.parent_group.expect("parent_group is not set"),
        };
        let args = RemoveGroupsFromGroupInstructionArgs {
            group_indices: self
                .group_indices
                .clone()
                .expect("group_indices is not set"),
            authority_indices: self
                .authority_indices
                .clone()
                .expect("authority_indices is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `remove_groups_from_group` CPI accounts.
pub struct RemoveGroupsFromGroupCpiAccounts<'a, 'b> {
    /// The parent group update authority or delegated authority
    pub authority: &'b solana_program::account_info::AccountInfo<'a>,
    /// The parent group account
    pub parent_group: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `remove_groups_from_group` CPI instruction.
pub struct RemoveGroupsFromGroupCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,
    /// The parent group update authority or delegated authority
    pub authority: &'b solana_program::account_info::AccountInfo<'a>,
    /// The parent group account
    pub parent_group: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: RemoveGroupsFromGroupInstructionArgs,
}

impl<'a, 'b> RemoveGroupsFromGroupCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: RemoveGroupsFromGroupCpiAccounts<'a, 'b>,
        args: RemoveGroupsFromGroupInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            parent_group: accounts.parent_group,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.parent_group.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = RemoveGroupsFromGroupInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::MPL_CORE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(2 + 1 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.parent_group.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `RemoveGroupsFromGroup` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[writable]` parent_group
pub struct RemoveGroupsFromGroupCpiBuilder<'a, 'b> {
    instruction: Box<RemoveGroupsFromGroupCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RemoveGroupsFromGroupCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RemoveGroupsFromGroupCpiBuilderInstruction {
            __program: program,
            authority: None,
            parent_group: None,
            group_indices: None,
            authority_indices: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The parent group update authority or delegated authority
    #[inline(always)]
    pub fn authority(
        &mut self,
        authority: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    /// The parent group account
    #[inline(always)]
    pub fn parent_group(
        &mut self,
        parent_group: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.parent_group = Some(parent_group);
        self
    }
    #[inline(always)]
    pub fn group_indices(&mut self, group_indices: Vec<u8>) -> &mut Self {
        self.instruction.group_indices = Some(group_indices);
        self
    }
    #[inline(always)]
    pub fn authority_indices(&mut self, authority_indices: Vec<u8>) -> &mut Self {
        self.instruction.authority_indices = Some(authority_indices);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = RemoveGroupsFromGroupInstructionArgs {
            group_indices: self
                .instruction
                .group_indices
                .clone()
                .expect("group_indices is not set"),
            authority_indices: self
                .instruction
                .authority_indices
                .clone()
                .expect("authority_indices is not set"),
        };
        let instruction = RemoveGroupsFromGroupCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            parent_group: self
                .instruction
                .parent_group
                .expect("parent_group is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

struct RemoveGroupsFromGroupCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    authority: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    parent_group: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    group_indices: Option<Vec<u8>>,
    authority_indices: Option<Vec<u8>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
